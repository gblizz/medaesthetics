import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
});

export const metadata: Metadata = {
  title: "MedAesthetics — Build Your Medical Aesthetics Practice",
  description:
    "The all-in-one practice management platform for Nurse Practitioners, PAs, and Physicians in medical aesthetics. Scheduling, charting, billing, and more.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} antialiased`}>{children}</body>
    </html>
  );
}
